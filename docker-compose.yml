version: '3.8'

services:
  backend:
    build: .
    ports:
      - "8081:8081"
    environment:
      - PORT=8081

  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    depends_on:
      - backend
